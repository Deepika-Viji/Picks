{
    "name": "picks_main_project",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "start": "cd backend && npm install && npm start"
    },
    "devDependencies": {
        "vite": "^6.3.1"
    },
    "dependencies": {
        "serve": "^14.2.4"
    }
}
